# Next.js app for the Sentry observability PoC.

Use the root README and `../scripts/start-all.sh`. Copy `../.env.example` to `../.env`, then the start script writes `.env.local` here so `NEXT_PUBLIC_*` values load.
